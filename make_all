#!/bin/bash

solvers=("Euler" "Euler_rot" "NS" "NS_rot" "QG" "QGf")

for str in ${solvers[@]}; do
  cd solvers/$str
  echo "COMPILING SOLVER: $str"
  make -f makefile_gnu $1
  echo "DONE"
  echo ""
  cd ../../
done
